<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="hr.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <block name="employees_setting_container" position="after">
                <block title="Shift Requests" name="hr_shift_request">
                    <setting
                        string="Urgent Swap Auto-Approval"
                        company_dependent="1"
                        help="When the offered shift starts within this many
                        hours, a swap accepted by the colleague is applied
                        immediately without waiting for the manager's
                        approval. Set to 0 to always require it."
                    >
                        <field name="swap_auto_approve_hours" />
                    </setting>
                </block>
            </block>
        </field>
    </record>
</odoo>
